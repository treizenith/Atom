{
	"name": "@treizenit/atomroot",
	"private": true,
	"devDependencies": {
		"lerna": "^3.22.1"
	},
	"scripts": {
		"pre": "lerna bootstrap",
		"start": "lerna exec --parallel -- npm run watch",
		"update": "lerna exec -- npm update",
		"build": "lerna run --parallel --stream build",
		"watch": "lerna run --parallel --stream watch"
	},
	"workspaces": [
		"core/*",
		"libs/*"
	]
}
