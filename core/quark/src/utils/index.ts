import pure from './pure';
import type from './type';
import merge from './merge';
import * as diff from './diff';
import cb from './cb';
import rn from "./rn";

export default {
	pure,
	type,
	merge,
	diff,
	cb,
	rn,
};
